//A importação do Reacy e do ReactDOM é impressindível.
import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; //Obrigatório, pois é onde está rodando a nossa aplicação.

ReactDOM.render(
    <App />,
  document.getElementById('root')
);


